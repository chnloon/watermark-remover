# 提审自检清单（LAUNCH-CHECKLIST.md）

> 小程序"链接解析"提审前逐条核对。带 ✅ 的条目代码已改好；带 ☐ 的需要你在微信后台 / 服务器上操作。
> 最后更新：2026-08-28（代码已上传版本 1.0.0，剩余均为微信后台手动操作）

---

## A. 代码层面（本次已完成的合规改造，自查确认）

- [x] tabBar / 导航栏命名为"链接解析"（原"去水印"已全部替换，`client/app.json`）
- [x] 页面标题、项目描述、根接口返回均中性化（`project.config.json`、`server/src/index.js`）
- [x] 新增《用户协议与隐私保护指引》页（`client/pages/agreement/`），首页底部有入口链接
- [x] 隐私授权弹窗：`__usePrivacyCheck__: true` + `onNeedPrivacyAuthorization` 处理（`client/app.json`、`client/app.js`），基础库 3.17.0 满足要求
- [x] 剪贴板改为**手动**读取（用户点"粘贴"才读，`onShow` 不再自动读取）
- [x] 保存视频/图片到相册前弹出版权提示，用户确认后才执行（`client/pages/result/result.js` 的 `confirmCopyright`）
- [x] 删除死代码：自动读剪贴板相关函数已清理（`detectClipboard` / `onParseClipboard` / `resolveDouyinShortLink`）
- [x] sitemap 仅收录首页，结果页/历史页/协议页不参与索引
- [x] 后端删除无鉴权调试接口 `/api/debug/lux`
- [x] 后端错误信息脱敏：不向客户端回传内部异常（统一"请重新解析后重试"）
- [x] 后端 SSRF 防护 / JWT 校验 / 限流 / CORS 白名单 / 隐藏 X-Powered-By 全部完成并本地验证通过

## B. 微信公众平台后台（需要你操作 ☐）

> ⚠️ **顺序很重要：必须先配置服务器域名 + 隐私保护指引，再提交审核。**
> 代码已通过开发者工具 CLI 上传（版本 `1.0.0`，770.7 KB，AppID `wx6af9c1f68a6bbd7e`），在后台"版本管理 → 开发版本"可见。

- [ ] **名称 / 头像 / 简介**确认无"去水印 / 无水印 / 下载视频"等敏感字眼（中性描述，如"输入链接，解析视频/图片"）
- [ ] **类目**选择合规（建议"工具 → 效率"或"工具 → 信息查询"；避免与"视频/影视"类目挂钩）
- [ ] **服务器域名**（开发管理 → 开发设置 → 服务器域名）：
  - request 合法域名：`https://yc0717.cc`
  - downloadFile 合法域名：`https://yc0717.cc`
  - 域名已验证：HTTPS 200、证书有效期至 2026-11-22（Let's Encrypt，审核期间不会过期）
  - 如配置第三方解析 API，一并添加其域名
- [ ] **用户隐私保护指引**（设置 → 服务内容声明 → 用户隐私保护指引）——代码已开启 `__usePrivacyCheck__: true`，**不配置则剪贴板/相册接口会被拦截**。按下表声明（与 `client/app.json` 的 permission.desc 及协议页一致）：

  | 收集的信息类型 | 用途说明 | 对应接口 |
  |---|---|---|
  | 剪贴板信息 | 仅在你主动点击"粘贴"时读取，用于解析你粘贴的分享链接 | `wx.getClipboardData` |
  | 相册（仅写入） | 仅在你主动点击"保存"时写入，用于将解析出的视频/图片存入你的相册 | `scope.writePhotosAlbum` |

  - 不需要声明：地理位置、手机号、微信昵称头像（代码均未使用）
- [ ] 小程序后台**版本管理**：确认开发版本 1.0.0 存在（CLI 已上传 ✅）→ 点"提交审核"时填写版本描述与审核备注

## C. 服务器部署（2026-08-28 已全部确认 ✅）

- [x] 后端跑在 **systemd**（`watermark.service`）下且已随开机自启（**注意：实际部署是 systemd，DEPLOY.md 里的 pm2 部分已过时**）
- [x] `server/.env` 存在且 `JWT_SECRET` 为强随机值（线上已核对，含 JWT_SECRET/JWT_EXPIRES_IN）
- [x] nginx + HTTPS 证书有效（`curl https://yc0717.cc/` 返回 200，证书 2026-08-24 ~ 2026-11-22）
- [x] 安全组**未**对 3001 端口放行公网访问（nginx 反代 443 → 3001）
- [x] SSRF 自测：`/api/download?url=http://127.0.0.1:3001/` 返回 400
- [x] 限流自测：连续请求出现 429
- [x] API 链路实测：`POST https://yc0717.cc/api/parse` 返回业务响应（非 404），nginx → 后端 → 解析逻辑全链路通

## D. 提审前最终检查（真机验证 ☐）

- [x] 开发者工具"上传"已成功，包体 770.7 KB < 2MB（CLI 上传版本 1.0.0，✅）
- [ ] 微信开发者工具"真机预览"，走完整流程：粘贴链接 → 解析 → 结果页播放 → 保存到相册（首次会弹版权提示 + 隐私授权弹窗，均能正常同意/拒绝）
- [ ] 协议页可从首页进入、内容完整可滚动
- [ ] 无网/弱网下解析失败提示友好（不白屏、不闪退）
- [ ] 审核备注填写：说明工具定位（帮助用户解析其有权使用的公开链接内容）、已配置隐私保护指引、无诱导分享

## E. 审核结果应对

| 审核结果 | 应对 |
|---|---|
| 通过 | 手动"发布"；上线后 1-2 天观察接口错误率与限流日志 |
| 被拒 | 把驳回理由截图发我；常见驳回（视频下载类）按 A 节继续弱化定位或用例说明 |
| 需补充材料 | 多为隐私指引/域名配置问题，对照 B/C 节补齐后重新提交 |
